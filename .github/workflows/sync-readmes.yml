name: 📄 Sync Project READMEs

on:
  schedule:
    - cron: '0 3 * * *'  # 매일 KST 12:00 (UTC 03:00)
  workflow_dispatch:  # 수동 실행 가능

jobs:
  sync-readmes:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout profile repo
        uses: actions/checkout@v4

      - name: Sync 3D Gallery README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/3Dgallery
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/3dgallery
          sparse-checkout: |
            README.md
            screenshots

      - name: Sync Chiwi Pixel RPG README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/chiwiPixelRpg
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/chiwi-pixel-rpg
          sparse-checkout: |
            README.md
            docs/screenshots

      - name: Sync KLS Connect README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/kls-connect
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/kls-connect
          sparse-checkout: |
            README.md
            docs/screenshots
            expo-app/screenshots

      - name: Sync WhaleFriend README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/whalefriend
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/whalefriend
          sparse-checkout: |
            README.md
            assets/icon.png
            docs/screenshots

      - name: Sync Card RPG README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/cardrpg
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/cardrpg
          sparse-checkout: |
            README.md

      - name: Sync AI Farm Simulation README
        uses: actions/checkout@v4
        with:
          repository: gogoonbuntu/aifarmsimu
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: _tmp/aifarmsimu
          sparse-checkout: |
            README.md
            screenshots

      - name: Copy READMEs and screenshots to projects/
        run: |
          # 3D Gallery
          [ -f _tmp/3dgallery/README.md ] && cp _tmp/3dgallery/README.md projects/3dgallery/README.md
          [ -d _tmp/3dgallery/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*.jpeg' --include='*.gif' --include='*/' --exclude='*' _tmp/3dgallery/screenshots/ projects/3dgallery/screenshots/

          # Chiwi Pixel RPG
          [ -f _tmp/chiwi-pixel-rpg/README.md ] && cp _tmp/chiwi-pixel-rpg/README.md projects/chiwi-pixel-rpg/README.md
          [ -d _tmp/chiwi-pixel-rpg/docs/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*/' --exclude='*' _tmp/chiwi-pixel-rpg/docs/screenshots/ projects/chiwi-pixel-rpg/docs/screenshots/

          # KLS Connect
          [ -f _tmp/kls-connect/README.md ] && cp _tmp/kls-connect/README.md projects/kls-connect/README.md
          [ -d _tmp/kls-connect/docs/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*/' --exclude='*' _tmp/kls-connect/docs/screenshots/ projects/kls-connect/docs/screenshots/
          [ -d _tmp/kls-connect/expo-app/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*/' --exclude='*' _tmp/kls-connect/expo-app/screenshots/ projects/kls-connect/expo-app/screenshots/

          # WhaleFriend
          [ -f _tmp/whalefriend/README.md ] && cp _tmp/whalefriend/README.md projects/whalefriend/README.md
          [ -f _tmp/whalefriend/assets/icon.png ] && cp _tmp/whalefriend/assets/icon.png projects/whalefriend/assets/icon.png
          [ -d _tmp/whalefriend/docs/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*/' --exclude='*' _tmp/whalefriend/docs/screenshots/ projects/whalefriend/docs/screenshots/

          # Card RPG
          [ -f _tmp/cardrpg/README.md ] && cp _tmp/cardrpg/README.md projects/cardrpg/README.md

          # AI Farm Simulation
          [ -f _tmp/aifarmsimu/README.md ] && cp _tmp/aifarmsimu/README.md projects/aifarmsimu/README.md
          [ -d _tmp/aifarmsimu/screenshots ] && rsync -a --delete --include='*.png' --include='*.jpg' --include='*/' --exclude='*' _tmp/aifarmsimu/screenshots/ projects/aifarmsimu/screenshots/

          # Cleanup
          rm -rf _tmp

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add projects/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📄 Sync project READMEs [automated]"
            git push
          fi
